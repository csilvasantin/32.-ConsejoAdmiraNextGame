.env
budget.json
__pycache__/
*.pyc
node_modules/

# capturas de prueba de tareas (runtime)
data/proofs/

# FleetControl — nunca commitear el token ni el log de auditoría
fleet-control/.fleet-token
fleet-control/audit.log

# council-api: log de auditoría del hackeo (runtime)
hackeo-audit.log

# identidad local por-máquina del tablero Vista Previa (no compartir)
vista-previa/quien.json
